const symbols=['🪙','⚓','☠️','🏴‍☠️','💎','🦜','🐙','🧭','💰','🌴','🍹','🚢','🦈','🐚','⭐','🔱','🏝️','🗝️','💀','🌊'];
const names=['Île du Départ','Baie des Palmiers','Île du Kraken','Fort des Flibustiers','Île au Trésor','Crique des Sirènes','Archipel Fantôme','Grotte du Kraken','Fort de la Tempête','Mer des Corsaires','Île des Épaves','Baie du Dragon','Citadelle du Requin','Volcan des Pirates','Grand Trésor'];
const counts=[0,48,60,72,84,96,108,120,132,144,144,144,144,144,144,144];
const difficulties=['','Facile','Facile','Moyen','Moyen','Difficile','Difficile','Très difficile','Très difficile','Expert','Expert','Maître','Maître','Légendaire','Mythique','Trésor ultime'];
const goals=[0,900,1150,1450,1750,2100,2450,2850,3250,3700,4200,4700,5200,5700,6200,6800];
const TILE_W=52,TILE_H=62,STEP_X=52,STEP_Y=62,LAYER_DX=26,LAYER_DY=31;
let state=JSON.parse(localStorage.getItem('mahjongPirateState')||'null')||{coins:0,gems:0,level:1,xp:0,bestScore:0,unlocked:1};
state.unlocked=Math.max(1,Math.min(15,Number(state.unlocked)||1));state.level=Math.max(1,Math.min(15,Number(state.level)||1));
let game=null,timer=null,startedAt=0;
const $=id=>document.getElementById(id);
function save(){localStorage.setItem('mahjongPirateState',JSON.stringify(state));updateHud()}
function captainTitle(){if(state.level<3)return'Mousse des Caraïbes';if(state.level<5)return'Capitaine Corsaire';if(state.level<8)return'Maître des Sept Mers';if(state.level<11)return'Légende des Océans';return'Roi des Flibustiers'}
function updateHud(){if(!$('coins'))return;$('coins').textContent=state.coins;$('gems').textContent=state.gems;$('captainName').textContent=captainTitle();$('level').textContent=state.level;$('xp').textContent=state.xp;$('bestScore').textContent=state.bestScore;const mp=$('mapProgress');if(mp)mp.textContent=`${state.unlocked}/15`;document.querySelectorAll('.island').forEach((b,i)=>{b.classList.toggle('locked',i+1>state.unlocked);b.classList.toggle('current',i+1===state.unlocked)})}
function rectOverlap(a,b){return a.x<b.x+TILE_W&&a.x+TILE_W>b.x&&a.y<b.y+TILE_H&&a.y+TILE_H>b.y}
function rectOverlapY(a,b){return a.y<b.y+TILE_H&&a.y+TILE_H>b.y}
function freeTile(t,tiles){if(t.removed)return false;for(const o of tiles)if(!o.removed&&o.l>t.l&&rectOverlap(t,o))return false;let left=false,right=false;for(const o of tiles){if(o.removed||o.l!==t.l||o.id===t.id||!rectOverlapY(t,o))continue;if(o.x<t.x&&o.x+TILE_W>t.x)left=true;if(o.x<t.x+TILE_W&&o.x+TILE_W>t.x+TILE_W)right=true}return !(left&&right)}
const SHAPE_PATTERNS={tortue:[2,4,6,6,6,6,6,4,2,2],pyramide:[2,4,6,6,6,6,6,6,6,6],diamant:[2,4,6,6,6,6,6,6,4,2],navire:[0,2,4,6,6,6,6,6,6,6],fort:[6,6,4,4,6,6,4,4,6,6],archipel:[4,6,6,4,6,4,6,6,6,4]};
const SHAPE_BY_LEVEL={1:'tortue',2:'pyramide',3:'diamant',4:'navire',5:'fort',6:'archipel'};
function shapeName(level){return SHAPE_BY_LEVEL[Math.min(level,6)]||'archipel'}
function patternCells(shape){const widths=SHAPE_PATTERNS[shape]||SHAPE_PATTERNS.archipel,cells=[];widths.forEach((width,r)=>{const start=Math.floor((6-width)/2);for(let c=0;c<width;c++)cells.push({r,c:start+c})});return cells}
function layerTargets(level){if(level===1)return[36,8,4,0,0];if(level===2)return[42,4,2,0,0];if(level===3)return[40,6,2,0,0];if(level===4)return[40,6,2,0,0];if(level===5)return[42,4,2,0,0];if(level===6)return[40,6,2,0,0];if(level===7)return[40,6,2,0,0];if(level===8)return[40,6,2,0,0];return[40,6,2,0,0]}
function layout(level){const targets=layerTargets(level),shape=shapeName(level),cells=patternCells(shape),boardW=6*STEP_X+TILE_W+18,boardH=10*STEP_Y+TILE_H+18,out=[];const baseX=(boardW-(6*STEP_X+TILE_W))/2,baseY=(boardH-(10*STEP_Y+TILE_H))/2;for(let l=0;l<5;l++){const wanted=Math.min(targets[l]||0,cells.length);if(!wanted)continue;if(l===0){const ranked=[...cells].sort((a,b)=>((a.c-2.5)**2+(a.r-4.5)**2)-((b.c-2.5)**2+(b.r-4.5)**2));const chosen=wanted===cells.length?cells:ranked.slice(0,wanted);chosen.sort((a,b)=>a.r-b.r||a.c-b.c).forEach(({r,c})=>out.push({x:baseX+c*STEP_X,y:baseY+r*STEP_Y,l}));continue}const sx=(l%2)*LAYER_DX;const candidates=cells.map(({r,c})=>({r,c,x:baseX+c*STEP_X+sx,y:baseY+4.5*STEP_Y+(r-4.5)*STEP_Y+l*LAYER_DY}));const ranked=candidates.sort((a,b)=>((a.c-2.5)**2+(a.r-4.5)**2)-((b.c-2.5)**2+(b.r-4.5)**2));const chosen=ranked.slice(0,wanted);chosen.sort((a,b)=>a.y-b.y||a.x-b.x).forEach(p=>out.push({x:p.x,y:p.y,l}))}return out.map((p,i)=>({...p,id:i}))}
function buildSolvable(level){const positions=layout(level),target=positions.length;for(let attempt=0;attempt<3000;attempt++){const remaining=positions.map(t=>({...t,removed:false})),pairs=[];while(remaining.some(t=>!t.removed)){const free=remaining.filter(t=>!t.removed&&freeTile(t,remaining));if(free.length<2)break;const a=free[Math.floor(Math.random()*free.length)];const others=free.filter(t=>t.id!==a.id);const b=others[Math.floor(Math.random()*others.length)];a.removed=true;b.removed=true;pairs.push([a.id,b.id])}if(pairs.length*2===target){const tiles=positions.map(p=>({...p,removed:false,symbol:null}));pairs.forEach((ids,i)=>ids.forEach(id=>tiles[id].symbol=symbols[i%symbols.length]));return tiles}}const tiles=positions.map(p=>({...p,removed:false,symbol:null}));for(let i=0;i<tiles.length;i+=2){const s=symbols[(i/2)%symbols.length];tiles[i].symbol=s;tiles[i+1].symbol=s}return tiles}
function symbolKind(s){const i=symbols.indexOf(s);return i<0?'unknown':`kind-${i}`}
function boardBaseWidth(){return 6*STEP_X+TILE_W+18}
function boardBaseHeight(){return 10*STEP_Y+TILE_H+18}
function newGame(level){level=Math.max(1,Math.min(15,Number(level)||1));clearInterval(timer);timer=null;closeDeadlock();closeLose();game={level,tiles:buildSolvable(level),selected:null,score:0,moves:0,pairs:0,combo:0,bestCombo:0,undo:[],shuffles:0};startedAt=Date.now();$('map').classList.add('hidden');$('game').classList.remove('hidden');$('islandTitle').textContent=names[level-1]||`Île ${level}`;$('difficulty').textContent=difficulties[level]||'Légendaire';$('msg').textContent='⚓ Trouve les paires de mêmes symboles.';render();timer=setInterval(tick,500);tick();window.scrollTo(0,0)}
window.newGame=newGame;
function tick(){if(!game)return;const sec=Math.floor((Date.now()-startedAt)/1000);$('time').textContent=`${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}
function updateTargets(){if(!game)return;const g=goals[game.level]||5000;if($('goal1'))$('goal1').textContent=Math.round(g*.3);if($('goal2'))$('goal2').textContent=Math.round(g*.6);if($('goal3'))$('goal3').textContent=g;if($('goalBar'))$('goalBar').style.width=Math.min(100,(game.score/g)*100)+'%'}
function render(){const board=$('board');if(!board)return;board.innerHTML='';board.style.width=boardBaseWidth()+'px';board.style.height=boardBaseHeight()+'px';board.dataset.shape=shapeName(game.level);board.dataset.level=game.level;for(const t of game.tiles){if(t.removed)continue;const free=freeTile(t,game.tiles),b=document.createElement('button');b.type='button';b.className='tile '+(free?'free':'blocked')+' '+symbolKind(t.symbol)+(game.selected===t.id?' selected':'');b.textContent=t.symbol;b.style.left=t.x+'px';b.style.top=t.y+'px';b.style.zIndex=20+t.l;b.dataset.layer=t.l;b.dataset.tileId=t.id;b.disabled=false;b.onclick=()=>pick(t.id);board.appendChild(b)}$('score').textContent=game.score;$('remaining').textContent=game.tiles.filter(t=>!t.removed).length;$('moves').textContent=game.moves;$('pairs').textContent=game.pairs;updateTargets();updateBoosters()}
function legalPairs(){if(!game)return[];const free=game.tiles.filter(t=>!t.removed&&freeTile(t,game.tiles)),out=[];for(let i=0;i<free.length;i++)for(let j=i+1;j<free.length;j++)if(free[i].symbol===free[j].symbol)out.push([free[i],free[j]]);return out}
function showDeadlock(){if(!game||game.tiles.every(t=>t.removed)||legalPairs().length)return;clearInterval(timer);timer=null;$('deadlockText').textContent=`Il reste ${game.tiles.filter(t=>!t.removed).length} tuiles, mais aucune paire libre n'est disponible. Utilise le mélange pour continuer.`;$('deadlock').showModal();sfx(false)}
function closeDeadlock(){if($('deadlock')?.open)$('deadlock').close()}
function closeLose(){if($('lose')?.open)$('lose').close()}
function showLose(){closeLose()}
function pick(id){if(!game)return;const t=game.tiles.find(x=>x.id===id);if(!t||t.removed||!freeTile(t,game.tiles))return;if(game.selected===id){game.selected=null;render();return}if(game.selected!==null){const a=game.tiles.find(x=>x.id===game.selected);if(a&&a.symbol===t.symbol&&freeTile(a,game.tiles)){game.undo.push({ids:[a.id,t.id],score:game.score,combo:game.combo});a.removed=true;t.removed=true;game.selected=null;game.moves++;game.pairs++;game.combo++;game.bestCombo=Math.max(game.bestCombo,game.combo);const gain=100+game.level*15+Math.max(0,game.combo-1)*20;game.score+=gain;sfx(true);$('msg').textContent=game.combo>=3?`🔥 Combo x${game.combo} ! +${gain} points.`:`⚓ Paire trouvée ! +${gain} points.`;render();if(game.tiles.every(x=>x.removed))win();else if(!legalPairs().length)showDeadlock();return}game.selected=null}$('msg').textContent=`🪙 ${t.symbol} sélectionnée — choisis la même tuile libre.`;game.selected=id;render()}
function hint(free=false){if(!game)return;const pair=legalPairs()[0];if(!pair){showDeadlock();return}game.selected=pair[0].id;if(!free)game.score=Math.max(0,game.score-10);render();$('msg').textContent=`💡 Indice : ${pair[0].symbol} a une paire libre.`}
function shuffle(free=false){if(!game)return;closeDeadlock();const active=game.tiles.filter(t=>!t.removed),original=active.map(t=>t.symbol);let ok=false;for(let attempt=0;attempt<250&&!ok;attempt++){const syms=[...original].sort(()=>Math.random()-.5);active.forEach((t,i)=>t.symbol=syms[i]);ok=legalPairs().length>0}if(!ok)active.forEach((t,i)=>t.symbol=original[i]);if(!free)game.score=Math.max(0,game.score-25);game.combo=0;game.selected=null;game.shuffles++;render();$('msg').textContent=ok?'🔀 Mélange : une paire est maintenant accessible.':'🔀 Le mélange n’a pas débloqué de paire.';timer=setInterval(tick,500)}
function undo(){if(!game||!game.undo.length){$('msg').textContent='↩️ Aucun coup à annuler.';return}const u=game.undo.pop();u.ids.forEach(id=>{const t=game.tiles.find(x=>x.id===id);if(t)t.removed=false});game.score=u.score;game.combo=u.combo;game.pairs=Math.max(0,game.pairs-1);game.moves++;game.selected=null;render();closeDeadlock();$('msg').textContent='↩️ Dernière paire restaurée.';if(!timer)timer=setInterval(tick,500)}
function bomb(){if(!game)return;const pair=legalPairs()[0];if(!pair){showDeadlock();return}pair.forEach(t=>t.removed=true);game.pairs++;game.score+=100;game.combo=0;game.selected=null;render();$('msg').textContent='💣 Canon pirate ! Une paire libre a été pulvérisée.';if(game.tiles.every(t=>t.removed))win()}
function updateBoosters(){if(!$('undoCount'))return;$('undoCount').textContent=game?.undo?.length||0;$('shuffleCount').textContent=game?.shuffles||0}
function win(){clearInterval(timer);timer=null;closeDeadlock();closeLose();const sec=Math.floor((Date.now()-startedAt)/1000),g=goals[game.level]||5000,stars=game.score>=g?3:game.score>=g*.6?2:1,reward=25*game.level+stars*10,xp=30*game.level+stars*12;state.coins+=reward;state.xp+=xp;state.level=Math.max(state.level,Math.min(15,1+Math.floor(state.xp/100)));if(game.score>state.bestScore)state.bestScore=game.score;if(game.level===state.unlocked&&state.unlocked<15)state.unlocked++;save();$('winText').textContent=`Île terminée en ${sec}s avec ${game.moves} coups et ${game.bestCombo} de combo.`;$('stars').textContent='★'.repeat(stars)+'☆'.repeat(3-stars);$('rewardCoins').textContent=reward;$('rewardXp').textContent=xp;$('next').textContent=game.level<15?'Île suivante':'Grand Trésor';$('next').disabled=game.level>=state.unlocked&&game.level>=15;$('win').showModal()}
function goHome(){clearInterval(timer);timer=null;game=null;$('game').classList.add('hidden');$('map').classList.add('hidden');$('home').classList.remove('hidden');if(typeof window.showPirateHome==='function')window.showPirateHome()}
function goNext(){if(!game)return;const next=Math.min(15,game.level+1);if(next>state.unlocked)return;$('win').close();newGame(next)}
function sfx(ok){try{const C=window.AudioContext||window.webkitAudioContext;if(!C)return;const c=new C(),o=c.createOscillator(),g=c.createGain();o.frequency.value=ok?620:180;o.type='sine';g.gain.setValueAtTime(.035,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+.12);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+.12)}catch(e){}}
$('homeBtn').onclick=goHome;$('hint').onclick=()=>hint(false);$('shuffle').onclick=()=>shuffle(false);$('restart').onclick=()=>newGame(game?.level||1);$('undo').onclick=undo;$('bomb').onclick=bomb;$('hint2').onclick=()=>hint(true);$('shuffle2').onclick=()=>shuffle(false);$('deadlockShuffle').onclick=()=>shuffle(true);$('deadlockRestart').onclick=()=>newGame(game?.level||1);$('loseUndo').onclick=undo;$('loseRestart').onclick=()=>newGame(game?.level||1);$('homeWin').onclick=goHome;$('next').onclick=goNext;
updateHud();